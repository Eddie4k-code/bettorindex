<section class="section section--cta">
  <div class="container">
    <div class="card cta">
      <img
        src={`${import.meta.env.BASE_URL}images/icon.png`}
        alt="BettorIndex app icon"
        width="64"
        height="64"
        class="cta__icon"
      />
      <h2 class="cta__title">Get BettorIndex</h2>
      <p class="cta__text">
        BettorIndex is currently in development for iOS. Android support is planned for a later release.
      </p>
      <div class="cta__badges">
        <span class="badge">Coming Soon — iOS</span>
        <span class="badge badge--muted">Android — Later</span>
      </div>
    </div>
  </div>
</section>

<style>
  .section--cta {
    padding-bottom: 80px;
  }

  .cta {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    padding: 48px 32px;
    background: radial-gradient(ellipse at 50% 100%, rgba(79, 195, 247, 0.06) 0%, var(--card) 60%);
  }

  .cta__icon {
    border-radius: 16px;
    margin-bottom: 20px;
  }

  .cta__title {
    margin: 0 0 12px;
    font-size: 28px;
    color: var(--text);
  }

  .cta__text {
    margin: 0 0 24px;
    color: var(--muted);
    font-size: 16px;
    max-width: 480px;
    line-height: 1.6;
  }

  .cta__badges {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
    justify-content: center;
  }
</style>
