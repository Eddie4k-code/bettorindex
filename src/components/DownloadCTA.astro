<section class="section section--cta">
  <div class="container">
    <div class="cta card">
      <div class="cta__glow" aria-hidden="true"></div>
      <img
        src={`${import.meta.env.BASE_URL}images/icon.png`}
        alt="BettorIndex app icon"
        width="72"
        height="72"
        class="cta__icon"
      />
      <p class="cta__eyebrow">Coming to iOS</p>
      <h2 class="cta__title">Get BettorIndex when it launches</h2>
      <p class="cta__text">
        BettorIndex is currently in development for iOS. Android support is planned for a later release.
      </p>
      <div class="cta__badges">
        <span class="badge">Coming Soon — iOS</span>
        <span class="badge badge--muted">Android — Later</span>
      </div>
    </div>
  </div>
</section>

<style>
  .section--cta {
    padding-bottom: 96px;
  }

  .cta {
    position: relative;
    overflow: hidden;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    padding: 56px 32px;
    background: linear-gradient(180deg, rgba(21, 29, 43, 0.98), rgba(17, 23, 34, 1));
  }

  .cta__glow {
    position: absolute;
    inset: auto 10% -30%;
    height: 220px;
    background: radial-gradient(circle, rgba(79, 195, 247, 0.18), transparent 70%);
    pointer-events: none;
  }

  .cta__icon {
    position: relative;
    z-index: 1;
    border-radius: 18px;
    margin-bottom: 18px;
    box-shadow: 0 18px 40px rgba(79, 195, 247, 0.22);
  }

  .cta__eyebrow {
    position: relative;
    z-index: 1;
    margin: 0 0 10px;
    color: var(--accent-bright);
    font-size: 12px;
    font-weight: 700;
    letter-spacing: 0.16em;
    text-transform: uppercase;
  }

  .cta__title {
    position: relative;
    z-index: 1;
    margin: 0 0 12px;
    font-family: var(--font-display);
    font-size: clamp(30px, 4vw, 38px);
    letter-spacing: -0.03em;
    color: var(--text);
  }

  .cta__text {
    position: relative;
    z-index: 1;
    margin: 0 0 24px;
    color: var(--muted);
    font-size: 16px;
    max-width: 520px;
    line-height: 1.7;
  }

  .cta__badges {
    position: relative;
    z-index: 1;
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
    justify-content: center;
  }
</style>
